package com.passione

import android.app.Application

class PassioneApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize any libraries here if needed
    }
}
